# =============================================================================
# TubeTest Tracker - Configuration
# =============================================================================
# Copiez ce fichier en .env et remplissez les valeurs

# =============================================================================
# BASE DE DONNÉES
# =============================================================================
# URL de connexion MySQL/MariaDB/PostgreSQL
# Format MySQL: mysql://user:password@host:port/database
# Format PostgreSQL: postgresql://user:password@host:port/database
DATABASE_URL=mysql://root:password@localhost:3306/tubetest_tracker

# =============================================================================
# AUTHENTIFICATION
# =============================================================================
# Clé secrète pour signer les tokens JWT (générez une chaîne aléatoire de 32+ caractères)
JWT_SECRET=votre_cle_secrete_jwt_tres_longue_et_aleatoire

# Mode d'authentification: "local" (email/password) ou "oauth" (Google OAuth)
AUTH_MODE=local

# Pour Google OAuth (optionnel si AUTH_MODE=local)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# =============================================================================
# OPENAI / LLM
# =============================================================================
# Clé API OpenAI pour la génération de contenu
OPENAI_API_KEY=sk-votre-cle-api-openai

# Modèle par défaut (gpt-4o, gpt-4o-mini, gpt-3.5-turbo)
OPENAI_MODEL=gpt-4o

# =============================================================================
# GOOGLE GEMINI (Alternative à OpenAI)
# =============================================================================
# Clé API Gemini (optionnel, utilisé si OPENAI_API_KEY n'est pas défini)
GEMINI_API_KEY=

# =============================================================================
# YOUTUBE API
# =============================================================================
# Clé API YouTube Data v3 (pour récupérer les vidéos et statistiques)
YOUTUBE_API_KEY=votre_cle_api_youtube

# OAuth YouTube (pour les fonctionnalités avancées)
YOUTUBE_CLIENT_ID=votre_client_id_youtube
YOUTUBE_CLIENT_SECRET=votre_client_secret_youtube

# =============================================================================
# STOCKAGE S3 (AWS ou compatible)
# =============================================================================
# Région AWS (ex: eu-west-1, us-east-1)
S3_REGION=eu-west-1

# Nom du bucket S3
S3_BUCKET=tubetest-tracker-uploads

# Credentials AWS
AWS_ACCESS_KEY_ID=votre_access_key_id
AWS_SECRET_ACCESS_KEY=votre_secret_access_key

# Endpoint S3 personnalisé (optionnel, pour MinIO ou autres services compatibles S3)
# S3_ENDPOINT=http://localhost:9000

# =============================================================================
# APPLICATION
# =============================================================================
# Port du serveur (défaut: 3000)
PORT=3000

# URL de base de l'application (pour les callbacks OAuth)
APP_URL=http://localhost:3000

# Titre de l'application
APP_TITLE=TubeTest Tracker

# Mode de l'environnement (development ou production)
NODE_ENV=development

# =============================================================================
# EMAIL (Optionnel - pour les notifications)
# =============================================================================
# Configuration SMTP pour l'envoi d'emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=votre_email@gmail.com
SMTP_PASSWORD=votre_mot_de_passe_application
SMTP_FROM=TubeTest Tracker <noreply@tubetest.com>

# =============================================================================
# ADMIN
# =============================================================================
# Email de l'administrateur (premier compte créé)
ADMIN_EMAIL=admin@example.com
# Mot de passe initial de l'administrateur (à changer après première connexion)
ADMIN_PASSWORD=admin123
